//! Search / unified-box / bench / transcripts CLI harness.
//!
//! Filter example: `cargo test -p kb-code-cli --test search_cli`

#[allow(unused)]
#[path = "../common/mod.rs"]
mod common;

mod bench_search;
mod search;
mod search_unified;
mod transcripts;
