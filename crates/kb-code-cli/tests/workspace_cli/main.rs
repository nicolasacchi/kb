//! Agent-view / bookmarks / hierarchy / impact-lenses / sets CLI harness.
//!
//! Filter example: `cargo test -p kb-code-cli --test workspace_cli`

#[allow(unused)]
#[path = "../common/mod.rs"]
mod common;

mod agentview;
mod bookmarks;
mod hierarchy;
mod impact_lenses;
mod sets;
