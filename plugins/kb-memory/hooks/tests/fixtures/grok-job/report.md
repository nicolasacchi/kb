# Report: fixture --dry-run flag

## Summary

Fixture-only report used by the `kb-capture-grok.sh` golden mapping test
(`crates/kb-cli/tests/adapter_grok.rs`) -- not real grokclaude output.

- Added `--dry-run` to the fixture `ExportCmd`.
- No new dependencies.
