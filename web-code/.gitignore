node_modules/
.npm-cache/
dist/
*.tsbuildinfo
.vite/
test-results/
playwright-report/
# e2e/package-lock.json IS tracked — `npm ci` (`just ci-code-e2e`) requires
# a committed lockfile, same discipline as the root tests/e2e's own.
