{
  "name": "kb-code-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright smoke for the kb-code Search-Everywhere box (omnibox + /search page) against a real kb-code-server daemon.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
