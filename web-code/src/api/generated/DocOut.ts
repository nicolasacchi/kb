// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Author } from "./Author";
import type { Card } from "./Card";
import type { CiOut } from "./CiOut";
import type { FieldRefs } from "./FieldRefs";
import type { FindingBrief } from "./FindingBrief";
import type { Flow } from "./Flow";
import type { Question } from "./Question";
import type { ReadingOrderOut } from "./ReadingOrderOut";
import type { Risk } from "./Risk";

/**
 * Everything `GET …/doc` returns, and the same object `compose` echoes.
 */
export type DocOut = { schema: string, review_id: number, repo: string, ps_number: number, revision: number, 
/**
 * How many revisions exist for this review across every patchset —
 * the append-only chain's true length.
 */
revisions: number, tier: string, created_at: number, 
/**
 * The lossless record: front matter + body, byte-for-byte as composed.
 */
doc_md: string, summary_md: string, 
/**
 * V76-B3 (kbc-prose/1) — `summary_md`'s prose refs, per request, never
 * persisted. Always present (empty when the summary carries none).
 */
summary_refs: FieldRefs, risk?: Risk, reading_order: ReadingOrderOut, 
/**
 * V73-K5 — the `ci:` block, authored-or-derived (same dual-source
 * pattern as `reading_order`).
 */
ci: CiOut, blocks: { [key in string]: string }, flows: Array<Flow>, questions: Array<Question>, author?: Author, findings: Array<FindingBrief>, 
/**
 * Every optional block this document does NOT carry. Always present,
 * even when empty — an absence is stated, never discovered.
 */
omitted: Array<string>, 
/**
 * `null` unless `?resolve=true`.
 */
cards?: Array<Card>, cards_resolved: boolean, };
