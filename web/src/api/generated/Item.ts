// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { InputView } from "./InputView";
import type { ResultView } from "./ResultView";
import type { ToolClass } from "./ToolClass";

export type Item = { "item": "Prose", text: string, } | { "item": "Thinking", empty: boolean, len: number, 
/**
 * The full text — `None` when `empty` (the hygiene-bundle glyph
 * path needs only the count; keeping `None` there avoids storing a
 * redundant empty string on the ~100% of thinking blocks that ARE
 * empty on Claude Code transcripts). `Some(text)` when non-empty,
 * so the presenter can still show a preview + full body exactly as
 * before — dropping this would regress "never worse than today"
 * for the harnesses (Grok Build) whose thinking blocks are real.
 */
text: string | null, } | { "item": "ToolCall", name: string, kind: ToolClass, headline: string, input_view: InputView, result: ResultView | null, unpaired: boolean, 
/**
 * The verbatim `file_path`/`notebook_path` for a file-op tool
 * (`Read`/`Write`/`Edit`/`NotebookEdit`/`NotebookRead`) — kept
 * alongside `headline` (which may already show it, truncated) so
 * the presenter can resolve it against the corpus mount table and
 * render a click-to-open link, exactly as the pre-IR renderer did.
 * `None` for every other tool (Bash args are deliberately never
 * linkified — false-positive risk).
 */
path_for_link: string | null, } | { "item": "Command", name: string, args: string | null, stdout: string | null, } | { "item": "TaskEvent", id: string, subject: string, transition: string, } | { "item": "WorkflowCard", name: string | null, description: string | null, phases: Array<string>, task_id: string | null, } | { "item": "KbCommand", verb: string, args: string, result_view: string | null, } | { "item": "MemoryInjection", items: Array<string>, } | { "item": "SystemReminder", preview: string, } | { "item": "Decision", prompt: string, answer: string | null, } | { "item": "ModeChange", mode: string, } | { "item": "TimeGap", secs: number, } | { "item": "Raw", reason: string, };
